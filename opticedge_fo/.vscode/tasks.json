{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start iOS Simulator",
      "type": "shell",
      "command": "flutter emulators --launch apple_ios_simulator || true",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
